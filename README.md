# Vanimel Panchayat Services Portal Demo

## Run locally
1. Install Node.js 18 or newer.
2. Open this folder in VS Code.
3. Run `npm install`.
4. Run `npm run dev`.
5. Open the local URL shown by Vite.

This prototype contains sample transport/service data. Replace it with verified Panchayat data before public deployment.
